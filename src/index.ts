import {encodeToString} from "./encode";
import {decodeFromString} from "./decode";
import {factory} from "./types/factory";

/*
TODO: move factory, lists to types. Import here, register with finder and build the big factory here.
 */
export {
  encodeToString,
  decodeFromString,
  factory
};